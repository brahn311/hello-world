git clone https://github.com/brahn311/hello-world.git

cd hello-world

# en la carpeta del repositorio y SIN --global para que sea solo local

git config user.name "brhan311"

git config user.email "abrahannn311@gmail.com"

git config --list

git status

git echo 'prueba_1' > test1

git add test1

git commit -m "it's only a test"

git commit -m "it's only a test"

git push origin master
